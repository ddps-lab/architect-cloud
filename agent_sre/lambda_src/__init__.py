"""lambda_src — SRE Incident Copilot 프레임워크.

이 패키지는 Lambda 함수 코드에 포함됩니다(콘솔에서 보입니다). 학생이 주로
편집하는 파일은 lambda_src/agent_app.py 입니다. 모듈 파일은 이름 앞의 m1~m5 로
어떤 실습 단계에 쓰이는지 표시돼 있습니다:

    m1_prompt.py / m1_model.py  — 모듈 1 (프롬프트 + 모델)
    m2_tools.py                 — 모듈 2 (운영 도구)
    m3_memory.py                — 모듈 3 (대화 기억)
    m4_knowledge_base.py        — 모듈 4 (회고 지식베이스 검색)
    m5_aws_docs.py              — 모듈 5 (AWS 공식문서 MCP)
"""
